# Set to "false" to switch from the in-memory mock data layer to a real backend.
# Any other value (including leaving this unset) keeps mocks on.
VITE_USE_MOCKS=false

# Base URL of the real backend API. Only used when VITE_USE_MOCKS=false.
VITE_API_BASE_URL=http://localhost:3000
